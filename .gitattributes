# Normalize line endings: text files are LF in the repo, checked out native on each OS.
* text=auto eol=lf

# Binary / generated assets — never normalize.
*.png binary
*.jpg binary
*.ico binary
*.svg text eol=lf

# Keep shell scripts LF even on Windows checkout.
*.sh text eol=lf
